{
  "tasks": [
    { "name": "dev", "commands": ["cd backend && npm run dev", "cd frontend && npm run dev"] },
    { "name": "migrate", "commands": ["cd backend && npx prisma migrate dev"] },
    { "name": "build", "commands": ["cd backend && npm run build", "cd frontend && npm run build"] },
    { "name": "test", "commands": ["cd backend && npm test"] }
  ]
}
